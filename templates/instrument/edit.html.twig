{% extends 'base.html.twig' %}

{% block title %}Edit Instrument{% endblock %}

{% block body %}
    <h1>Modification de l'instrument</h1>

    {{ include('instrument/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_instrument_index') }}" class="btn btn-secondary">
        Retour à la liste
    </a>

    {{ include('instrument/_delete_form.html.twig') }}
{% endblock %}
