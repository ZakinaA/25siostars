<form method="post" action="{{ path('app_eleve_delete', {'id': eleve.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eleve.id) }}">
    <button class="btn">Supprimer</button>
</form>
